meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/api/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email" : "lgf@email.com",
    "password" : "12345678"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("ACCESS_TOKEN", res.body.accessToken);
  }
}
